# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Runtime do harness (gerados em execução; não versionar).
# O pidfile e o lock continuam nascendo AQUI: o motor (repo hii) os resolve por
# HICODE_RUNNER_PIDFILE/HICODE_RUNNER_LOCK, que apontam para esta raiz via .hii-env.sh.
*.log
.runner.pid
.runner.pid.root
.runner.lock
.claude/worktrees/
cards/runs/
cards/previews/

# Nuxt
panel/.nuxt/
panel/.output/
panel/.data/

# Dependências / segredos
node_modules/
.env
.env.*
cards/refs/

# Configuração local da máquina (caminhos do clone-alvo) — nunca versionar.
# Template versionado: config/repos.example.json
config/repos.json

# preferencia de IA em tempo de execucao (por maquina)
config/ia.json

# contrato de ambiente local (caminhos desta maquina)
.hii-env.sh
